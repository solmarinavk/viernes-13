[build]
  publish = "."
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    # sin caché: cada visita trae siempre la última versión (evita imágenes/HTML viejos)
    Cache-Control = "public, max-age=0, must-revalidate"
